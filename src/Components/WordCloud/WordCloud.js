import React, { useEffect } from "react";
import "./WordCloud.scss";

const WordCloud = function ({ topics }) {
  useEffect(() => {}, []);

  return <section className="word-cloud-main">Word Cloud</section>;
};

export default WordCloud;
